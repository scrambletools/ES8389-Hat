# Scramble Hat-trik — Fabrication & Assembly Files

Production files for **JLCPCB** (PCB fab + PCBA assembly). Board is an ES8389
audio codec HAT for the Waveshare ESP32-P4-Nano carrier (XLR/¼" combo input,
PoE-tapped phantom power, TRRS output, I²S/I²C to the carrier).

- **Board size:** ~51.4 × 21.7 mm
- **Layers:** 4 (F.Cu / In1.Cu / In2.Cu / B.Cu)
- **Assembly:** 28 BOM line items (48 placements/board) machine-assembled;
  3 connectors + 1 Y-cap hand-soldered; 1 part unpopulated.

> Regenerate everything with **`python3 tools/gen_fab.py`** (see the bottom).
> These files are committed from the design in the repo root; don't hand-edit them.

---

## Files

| File | Purpose |
|---|---|
| `scramble_hat-gerbers.zip` | **PCB fab** — all gerbers + drills (upload this) |
| `scramble_hat-*.gbr`, `*.drl`, `*.gbrjob` | Loose copies of the layers/drills in the zip |
| `scramble_hat-BOM.csv` / `.xlsx` | **Assembly BOM** — JLCPCB upload format (Comment, Designator, Footprint, LCSC#) |
| `Sample-BOM_JLCSMT.xlsx`, `JLCSMT_Sample_CPL1.xlsx` | JLCPCB's blank format templates (reference only) |
| `scramble_hat-CPL.csv` | **Placement file** — generated by `tools/gen_fab.py` with JLCPCB rotation corrections + the U1 fix baked in (still verify — see below) |

---

## How to order

### 1. PCB fabrication
Upload **`scramble_hat-gerbers.zip`**. Set **4 layers**; the rest can be
JLCPCB defaults (1.6 mm, HASL/ENIG, no controlled impedance required).

### 2. PCB assembly (PCBA)
1. **Enable "PCBA"** and turn on **through-hole / THT component assembly** at
   checkout — several parts are through-hole (see the assembled-THT list below).
2. Upload the **BOM**: `scramble_hat-BOM.csv`.
3. Upload the **CPL / placement file** — **you must generate this** (see next section).

### 3. ⚠️ CPL / placement — verify rotations before ordering
`scramble_hat-CPL.csv` is generated by `tools/gen_fab.py`. Its rotations were
**cross-validated against the KiCad Fabrication Toolkit plugin** — they match on
every placed part — and it additionally corrects **U1** (`+270°`), which the
plugin's auto-database misses because of the custom `ES8389_QFN24_P0.35mm` name.

Even so, **verify every rotation in JLCPCB's assembly preview** before ordering —
especially **U1 (QFN)**, whose correction is a best estimate. The preview overlays
each part on its land; rotate anything that looks off.

Parts carrying a rotation correction (double-check these first): `U1`, `U2`, `Q1`,
`D1`, `D2`, `OK1`. The CPL already excludes the hand-solder (J1/J2/J5) and DNP
(R14) parts via their `exclude_from_pos_files` / `dnp` PCB flags.

---

## Who solders what

| Set | Parts | Notes |
|---|---|---|
| **JLCPCB — SMT** | everything not listed below | machine-placed |
| **JLCPCB — THT** (enable at checkout) | `J3`, `J4` (carrier sockets), `SW1` (slide switch), `J6` (I²C header) | wave/hand-soldered by JLCPCB |
| **You — hand-solder after arrival** | `J1` (XLR/¼" combo jack), `J2` (TRRS jack), `J5` (PoE header), `C19` (Y2 safety cap) | J2 is out of stock at LCSC (source separately); J5 is where you wire the PoE module; C19 is a user-sourced 7.5 mm Y2 disc (Murata DE2E3 / TDK CS11) — kept off the JLC line so its niche stock never blocks the order |
| **Unpopulated (DNP)** | `R14` | 0 Ω ground-lift jumper, deliberately left off |

---

## Sourcing notes
- **F1** polyfuse (`1812L010/60DR`, C207062) — stock is thin (~120); re-check at order time. It's the voltage-critical 60 V part — do not substitute a lower-voltage 1812.
- Extended-library parts (U1, EL3H7 opto, TVS, R16, the 6.81 k, J6, etc.) each carry JLCPCB's one-time extended-part fee.
- Per-board BOM parts ≈ **$8** at qty-10 pricing (dominated by the J1/J2 jacks); budget more for the one-time extended fees.

---

## Regenerating these files
From the repo root, with KiCad closed:

```sh
python3 tools/gen_fab.py
```

One script rebuilds the gerbers, drills, zip, **BOM** (from the root `BOM.csv` —
single source of truth — with DNP rows dropped) and the **CPL** (with JLCPCB
rotation corrections). Needs `kicad-cli` on PATH and `openpyxl` for the `.xlsx`
BOM. The rotation-correction table is at the top of the script — extend it when
you add parts, and always re-verify in JLCPCB's preview.
