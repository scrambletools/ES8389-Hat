*.bak*
.history/
agents.md
project.md

# KiCad-generated / per-user churn
scramble_hat-backups/
*.kicad_prl
*-drc.rpt
*-erc.rpt

# Board-level STEP exports (whole-board kicad-cli output).
# Footprint models in lib/3dmodels/ and case geometry in case/ stay tracked.
/*.step
/*.stp

# Plasticity churn — keep only the main model (case/hat-trik-case.plasticity);
# ignore autosave and the rolling hour/day/prev history snapshots.
*_autosave.plasticity
case/plasticity/
